# Reserved for a future server-side speech/evaluation integration.
# The current browser POC does not read these values and requires no secrets.
API_BASE_URL=http://localhost:3000
ENABLE_MOCK_EVALUATION=true
STT_API_KEY=replace_with_your_key
